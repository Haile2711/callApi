export const { API_ENDPOINT } = process.env;
export const { BASE_URL } = process.env;
export const API_TIMEOUT = Number(process.env.API_TIMEOUT);
export const CODE_SUCCESS = Number(process.env.CODE_SUCCESS);
export const CODE_TIME_OUT = Number(process.env.CODE_TIME_OUT);
export const EXPIRED_TOKEN = Number(process.env.EXPIRED_TOKEN);
export const { NETWORK_ERROR } = process.env;
export const { TIME_ERROR } = process.env;
